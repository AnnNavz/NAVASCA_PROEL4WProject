@page
@model NAVASCA_PROEL4WProject.Pages.RegisterShops.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RegisterShop[0].ShopName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RegisterShop[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RegisterShop[0].PickupAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RegisterShop[0].ContactNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RegisterShop[0].Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.RegisterShop) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ShopName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PickupAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContactNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ShopID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ShopID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ShopID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
